$schema: https://azuremlschemas.azureedge.net/promptflow/latest/Run.schema.json
flow: ../../eval_flow
column_mapping:
  chat_history: ${run.inputs.question}
  question: ${run.inputs.question}
  answer: ${run.outputs.answer}
  

